import type { ThemeConfig } from "antd"

export const theme = {
  components: {
    Notification: {
      borderRadiusLG: 20,
      width: 420,
      paddingMD: 24,
      paddingContentHorizontal: 24,
    },
  },
} satisfies ThemeConfig
